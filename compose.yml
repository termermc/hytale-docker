services:
    hytale:
        image: ghcr.io/termermc/hytale-server:latest
        build:
            context: .
            dockerfile: ./Dockerfile

        tty: true
        stdin_open: true

        network_mode: host

        volumes:
         - ./runtime:/data/hytale/runtime
